#include "main.h>"
/**
 * print_sign -> prints sign of number
 * @n: parameter
 * Return: + for positive, 0 for zero and - for negative
 */
int print_sign(int n)
{
	if (n < 0)
		return (+);
	else if (n == 0)
		return (0);
	else
		return (-);
}
