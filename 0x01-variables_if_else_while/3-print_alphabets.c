#include <stdio.h>
/**
 * main - lower and upper case
 * Return: Always 0.
 */
int main(void)
{
char letter;
for (letter = 'a'; i <= 'z'; letter++)
putchar(letter);
for (letter = 'A'; j <= 'Z'; letter++)
putchar(letter);
putchar('\n');
return (0);
}
